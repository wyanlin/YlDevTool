## 2025-12-29

- 将信息类型输入框改为下拉选择框，包含预设的协议类型选项：
  - 0x07 - AT命令
  - 0x0F - 短信
  - 0x13 - 语音
  - 0x17 - 数据
  - 0x1F - AQ指令
- 更新事件监听器，为select元素使用change事件而不是input事件
- 在页面右下角添加更新时间显示，每次修改时更新为当前时间
- 优化footer样式，支持绝对定位的更新时间显示

## 2025-11-13

- 增强HHGL协议组包功能，支持ASCII格式输入自动转换为十六进制
- 改进前缀/后缀选择选项，现在支持前缀和后缀的独立选择：
  - 前缀 \r：在内容前添加回车符
  - 后缀 \r：在内容后添加回车符
  - 前缀 \r\n：在内容前添加回车换行符
  - 后缀 \r\n：在内容后添加回车换行符
- 设置HHGL协议组包的默认选项：信息内容格式默认为ASCII，前缀/后缀默认为"后缀 \r"
- 将信息内容输入框的默认placeholder文本设置为"AT+CGMR"
- 将HHGL协议组包/解包功能设为工具菜单的第一项和默认激活页面
- 更新组包界面布局，添加输入格式选择器和前缀/后缀选择器
- 优化界面样式，支持新的表单控件并保持一致的设计风格

## 2025-11-07

- 创建 `index.html`、`styles.css` 与 `main.js`，实现十六进制与 ASCII 双向转换的网页工具，并提供复制、清空等便捷交互。
- 扩展页面，新增协议组包与解包验证模块，支持配置起始标识、信息类型、帧计数与负载，自动生成或解析数据帧并提示字段含义。
- 优化协议解包时的 ASCII 预览，将控制字符显示为转义形式（如 `\r`、`\n`、`\t`），便于比对帧内容。
- 解包结果中信息类型字段显示对应注释（如 AT 命令、短信等），未知类型会有默认提示。
- 修正 AT 命令信息类型为 `0x07`，并同步更新默认输入与解析映射。
- 引入顶部工具切换菜单，将“十六进制 ⇄ ASCII”与“协议组包 / 解包”分置独立视图，便于快速选择所需工具。
- 改造成侧边工具菜单，可随时扩展新功能，并在切换时同步更新主标题和说明文本。
- 调整布局宽度与居中策略：菜单固定靠左，操作区域居中显示并扩大可视宽度，提升使用体验。

